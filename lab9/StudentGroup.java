import java.util.*;
public class StudentGroup {
    ArrayList<Student> group = new ArrayList();
    StudentGroup() {
        //Student st = new Student();
        //group.add({"default group", "r", "f"});
    }
    StudentGroup(ArrayList<Student> group) {
        this.group = group;
    }
}
